function myFunction() {
    var x = document.getElementById("userName").value;
    document.getElementById("demo").innerHTML = x;

    // TO-DO: connect backend with frontend to allow the entry of the username into the database
  }